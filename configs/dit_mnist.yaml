training:
  epochs: 30
  batch_size: 128
  learning_rate: 0.0005
  warmup_epochs: 5

model:
  type: dit
  hidden_dim: 64
  num_heads: 4
  num_layers: 6
  patch_size: 4
  in_channels: 1
  image_size: 28